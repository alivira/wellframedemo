<%= stylesheet_link_tag "header" %>

<nav class="navbar navbar-default" role="navigation">
  <div style="background-color:#FF6400;" class="container-fluid">
      <%=image_tag "wellframe-logo.png", :class => "style_image"%>

      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
            <% if current_user.admin %>
              <li><%= link_to "Patients", patients_path, :class => "one" %></li>
            <% end %>
            <li><%= link_to "Care Managers", care_managers_path, :class => "one" %></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle one" data-toggle="dropdown">Account<span class="caret"></span></a>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
            <% else %>
              <li><%= link_to "Sign in", signin_path, :class => "one" %></li>
          <% end %>
          </ul>
        </li>
      </ul>
  </div><!-- /.container-fluid -->
</nav>